import { QuotePipePipe } from './quote-pipe.pipe';

describe('QuotePipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuotePipePipe();
    expect(pipe).toBeTruthy();
  });
});
